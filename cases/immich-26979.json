{
  "id": "immich-26979",
  "upstream_repo": "immich-app/immich",
  "issue": 26979,
  "base_sha": "0687c0d3f76a075cf86ae5e28f1ec3ee3b355942",
  "ground_truth_pr": 30601,
  "track": "mobile-runtime-state",
  "difficulty": "medium",
  "contract": "After opening an asset from the mobile map and returning, moving or zooming the map must continue updating the visible nearby assets without reopening the map view.",
  "ground_truth_policy": "Do not inspect the upstream fix diff before the candidate solution has been evaluated.",
  "status": "runnable",
  "runtime": {
    "kind": "flutter",
    "version": "3.44.8",
    "working_directory": "mobile"
  },
  "validation": {
    "setup": ["flutter pub get"],
    "commands": ["flutter test"],
    "notes": "Start with the repository-native mobile test suite. Once a candidate fix exists, add the narrowest regression test command if a focused test can be identified without revealing the upstream fix implementation."
  }
}
